name: Build TLS Scanner Image

on:
  schedule:
    - cron: '23 4 * * *'
  workflow_dispatch:

permissions: read-all

env:
  REGISTRY: quay.io
  IMAGE_NAME: bapalm/tls-scanner

jobs:
  build:
    name: Build and Push TLS Scanner
    runs-on: ubuntu-latest
    permissions:
      packages: write

    steps:
      - name: Checkout upstream tls-scanner
        uses: actions/checkout@v6
        with:
          repository: openshift/tls-scanner

      - name: Patch Dockerfile.local for Fedora base and correct Go version
        run: |
          GO_VERSION=$(grep '^go ' go.mod | awk '{print $2}')
          sed -i "s|golang:1\.[0-9]*|golang:${GO_VERSION}|" Dockerfile.local
          sed -i 's|registry.access.redhat.com/ubi9/ubi:latest|registry.fedoraproject.org/fedora:latest|' Dockerfile.local
          grep -q 'registry.fedoraproject.org/fedora:latest' Dockerfile.local || { echo "FATAL: Fedora base image sed failed"; exit 1; }
          grep -q "golang:${GO_VERSION}" Dockerfile.local || { echo "FATAL: Go version sed failed"; exit 1; }
          echo "Patched Dockerfile.local: Go ${GO_VERSION}, Fedora base"
          head -5 Dockerfile.local

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to Quay.io
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.QUAY_USERNAME }}
          password: ${{ secrets.QUAY_PASSWORD }}

      - name: Build and push multi-arch image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.local
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha,scope=tls-scanner
          cache-to: type=gha,mode=max,scope=tls-scanner
